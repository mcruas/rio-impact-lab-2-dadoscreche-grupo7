{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "endereco.schema.json",
  "title": "Endereco",
  "type": "object",
  "required": ["bairro"],
  "properties": {
    "tipo": {
      "type": "string",
      "minLength": 1,
      "description": "Papel deste endereço para o responsável (usado pelo motor de recomendação de escola para calcular proximidade). 'Moradia' é obrigatório em pelo menos um item; 'Trabalho' é o outro valor comum, mas aceita qualquer texto livre para locais de desejo adicionais informados pela família (ex.: 'Avós', 'Escola atual')."
    },
    "logradouro": { "type": "string" },
    "numero": { "type": "string" },
    "complemento": { "type": "string" },
    "bairro": { "type": "string", "minLength": 1 },
    "cep": { "type": "string", "pattern": "^[0-9]{8}$" }
  },
  "additionalProperties": false
}
